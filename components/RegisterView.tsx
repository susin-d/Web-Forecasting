import React from 'react';

// This component is no longer used as the registration functionality has been removed.
const RegisterView: React.FC = () => {
  return null;
};

export default RegisterView;
